{{define "content"}}
	{{with .PageData}}

	<div class="container">

		{{template "current_and_next" .}}

	</div><!-- /.container -->

	<div class="container">

		{{range .Banners}}
		<a href="{{.Target}}">
			<img alt="{{.Alt}}" src="https://ury.org.uk{{.URL}}">
		</a>
		{{end}}

	</div>

	{{end}}
{{end}}
