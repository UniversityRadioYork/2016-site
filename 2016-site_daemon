#!/bin/sh

# $FreeBSD$
#
# PROVIDE: 2016-site
# KEYWORD: shutdown
#
# This file runs 2016-site as a service on a FreeBSD system.
# Modify paths as necessary and install in /usr/local/etc/rc.d/
#
# Add the following lines to /etc/rc.conf.local or /etc/rc.conf
# to enable this service:
#
# 2016-site_enable (bool): Set to NO by default.
#                  Set it to YES to enable hawk.

. /etc/rc.subr

name=2016-site
rcvar=2016-site_enable
2016-site_enable=${2016-site_enable:-"NO"}

2016-site_chdir='/usr/local/www/2016-site' # TODO: This shouldn't be necessary

command='/usr/local/www/2016-site/2016-site'
pidfile='/var/run/2016-site.pid'

start_cmd="/usr/sbin/daemon -u website -p ${pidfile} ${command}"

load_rc_config "$name"
run_rc_command "$1"
